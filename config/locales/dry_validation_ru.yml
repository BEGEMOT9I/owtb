ru:
  errors:
    check_email?: "Уже существует"
    or: "или"
    array?: "должно быть массивом"

    empty?: "не должно быть пустым"

    excludes?: "не должно содержать %{value}"

    excluded_from?:
      arg:
        default: "не должно быть одним из: %{list}"
        range: "не должно быть одним из: %{list_left} - %{list_right}"
    exclusion?: "не должно быть одним из: %{list}"

    eql?: "должно соответствовать %{left}"

    not_eql?: "не должно соответствовать %{left}"

    filled?: "должно быть заполненным"

    format?: "не соотвествует формату"

    number?: "должно быть числом"

    odd?: "должно быть четным"

    even?: "должно быть нечетным"

    gt?: "должно быть больше %{num}"

    gteq?: "должно быть больше или равно %{num}"

    hash?: "должно быть объектом"

    included_in?:
      arg:
        default: "должно быть одним из: %{list}"
        range: "должно быть одним из: %{list_left} - %{list_right}"
    inclusion?: "должно быть одним из: %{list}"

    includes?: "должно содержать %{value}"

    bool?: "должно быть булевым типом"

    true?: "должно быть true"

    false?: "должно быть false"

    int?: "должно быть целым числом"

    float?: "должно быть десятичной дробью"

    decimal?: "должно быть десятичной дробью"

    date?: "должно быть датой"

    date_time?: "должно быть временем"

    time?: "должно быть временем"

    key?: "отсутствует"

    attr?: "отсутствует"

    lt?: "должно быть меньше %{num}"

    lteq?: "должно быть меньше или равно %{num}"

    max_size?: "размер не должен быть больше %{num}"

    min_size?: "размер не должен быть меньше %{num}"

    none?: "не должно быть определено"

    str?: "должно быть строкой"

    type?: "должно быть %{type}"

    size?:
      arg:
        default: "размер должен соответствовать %{size}"
        range: "размер должен быть в пределах %{size_left} - %{size_right}"

      value:
        string:
          arg:
            default: "длинна должна соответствовать %{size}"
            range: "длинна должна быть в пределах %{size_left} - %{size_right}"