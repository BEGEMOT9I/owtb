{
  "name": "owtb",
  "private": true,
  "scripts": {
    "postinstall": "cd client && yarn install && cd ..",
    "build": "cd client && yarn build:prod",
    "lint": "cd client && yarn lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run lint"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^1.1.0",
    "swagger-cli": "^2.2.0"
  }
}
